export type ResponseItems = {
    result: Item[];
};
export type ResponseIds = {
    result: string[];
};

export type Item = {
    brand: string;
    id: string;
    price: number;
    product: string;
};
